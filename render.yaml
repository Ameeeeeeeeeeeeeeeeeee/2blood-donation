services:
  # Backend Service
  - type: web
    name: blood-donation-backend
    runtime: python
    buildCommand: ./build.sh
    startCommand: gunicorn config.wsgi:application
    envVars:
      - key: DATABASE_URL
        sync: false # Set this in Render dashboard from your Supabase Connection String
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "*"
    rootDir: backend

  # Frontend Service
  - type: web
    name: blood-donation-frontend
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    rootDir: frontend
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: blood-donation-backend
          property: url # Using public URL instead of internal host
